= content_for :head do
  %title= "#{@practice.company.name}'s practices regarding #{@practice.guideline.name}"

.container
  %h1
    ="#{@practice.company.name}'s practices"

  %h2
    Guideline

  .panel.panel-default
    .panel-heading
      %h3.panel-title=@practice.guideline.name
    .panel-body
      %p= @practice.guideline.description
      %table.table
        %tr.info
          %th Criteria
          %th Result
        %tr
          %td= @practice.guideline.true_description
          %td ... in order to 'follow' this guideline

        %tr
          %td= @practice.guideline.false_description
          %td ... in order to 'not follow' this guideline

      %p
        For more details,
        =link_to "read about this guideline.", @practice.guideline

  %h2 Implementation
  .panel.panel-default
    .panel-heading
      %h3.panel-title= "How does #{@practice.company.name} implement #{@practice.guideline.name}?"
    .panel-body
      %p
        ="#{@practice.company.name}'s implementation: "
        %span.btn.btn-default.implementation_status=@practice.implementation_text
