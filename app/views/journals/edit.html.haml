.container
  = content_for :head do
    %title= "Edit #{@journal.name}"

  .page-header
    %h1
      = @journal.name
      %small Edit Info:    

  = render 'form'

  = link_to 'Back', @journal, {class: 'btn btn-default'}
